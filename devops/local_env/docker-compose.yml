version: '3.1'

services:
  postgres:
    image: postgres:14.1-alpine
    container_name: db_novoanjo
    restart: always
    environment:
      POSTGRES_PASSWORD: jesus
      POSTGRES_DB: novoanjo
    ports:
      - "5432:5432"
    networks:
      - novoanjo-network

networks:
  novoanjo-network:
    driver: bridge
